# Shannon 中文增强版使用说明

这个版本在原版 Shannon 基础上做了三件事：

1. 默认生成中文安全报告，保留漏洞编号、路径、命令、Payload 等技术证据。
2. 增加本地中文监控端，可以用浏览器查看扫描状态、日志、报告文件和主报告预览。
3. 支持 GPT、DeepSeek 以及其他 OpenAI 兼容接口的一键启动。

## 适合谁用

适合代码审计、授权渗透测试、内部安全验收。不要拿去扫没有授权的网站。

## 准备工作

需要先安装：

- Docker Desktop
- Node.js 18 或以上
- pnpm

如果使用 DeepSeek，准备一个 DeepSeek API Key。如果使用 GPT，准备 OpenAI API Key。

## 最简单启动方式

在 PowerShell 里进入项目目录：

```powershell
cd D:\工具集\AI渗透工具\shannon-1.2.0
```

用 DeepSeek 扫描：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start-shannon.ps1 `
  -Provider deepseek `
  -ApiKey "你的DeepSeekKey" `
  -Url "http://127.0.0.1:8088" `
  -Repo "D:\codex\targets\v2" `
  -Workspace "v2-cms-scan" `
  -Output "D:\codex\reports\v2-cms-scan" `
  -Monitor
```

启动后会打开中文监控端：

```text
http://127.0.0.1:8787
```

如果没有使用 `-Monitor`，也可以手动启动监控端：

```powershell
node apps/cli/dist/index.mjs monitor v2-cms-scan
```

## 用 GPT 或其他 OpenAI 兼容接口

GPT：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start-shannon.ps1 `
  -Provider openai `
  -ApiKey "你的OpenAIKey" `
  -Url "http://127.0.0.1:8088" `
  -Repo "D:\codex\targets\v2" `
  -Workspace "gpt-scan" `
  -Monitor
```

OpenAI 兼容接口：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start-shannon.ps1 `
  -Provider openai-compatible `
  -ApiKey "你的Key" `
  -BaseUrl "https://api.example.com/v1" `
  -SmallModel "deepseek-chat" `
  -MediumModel "deepseek-chat" `
  -LargeModel "deepseek-reasoner" `
  -Url "http://127.0.0.1:8088" `
  -Repo "D:\codex\targets\v2" `
  -Workspace "compat-scan" `
  -Monitor
```

如果你的网络需要代理：

```powershell
-OutboundProxy "http://127.0.0.1:10809"
```

## 扫描结果在哪里

主报告：

```text
workspaces\<workspace>\deliverables\comprehensive_security_assessment_report.md
```

如果传了 `-Output`，报告也会复制到你指定的目录。

常用报告文件：

- `comprehensive_security_assessment_report.md`：总报告
- `injection_exploitation_evidence.md`：注入/RCE 证据
- `xss_exploitation_evidence.md`：XSS 证据
- `auth_exploitation_evidence.md`：认证问题证据
- `authz_exploitation_evidence.md`：授权问题证据
- `ssrf_exploitation_evidence.md`：SSRF 证据

## 常用命令

查看日志：

```powershell
node apps/cli/dist/index.mjs logs v2-cms-scan
```

打开中文监控端：

```powershell
node apps/cli/dist/index.mjs monitor v2-cms-scan
```

指定端口：

```powershell
node apps/cli/dist/index.mjs monitor v2-cms-scan --port 8899
```

停止容器：

```powershell
node apps/cli/dist/index.mjs stop --clean
```

## 注意事项

- 扫描前确认你有授权。
- 扫描过程中可能会修改测试环境数据，例如后台配置、广告代码、统计代码、锁定文件。
- 测试结束后应检查目标项目里是否残留测试 payload。
- 如果报告里出现“环境阻断”，意思是代码有风险，但当前部署条件让它暂时打不通，例如缺少 `php-curl`。
- 如果模型额度不足，先换可用 Key 或减少测试范围。

## 建议流程

1. 本地或虚拟机启动目标 CMS。
2. 固定一个 workspace 名称。
3. 用 `start-shannon.ps1` 启动扫描并加 `-Monitor`。
4. 在 `http://127.0.0.1:8787` 看实时日志。
5. 扫描完成后先看总报告，再看分项证据。
6. 按“已验证可利用”优先修复。
